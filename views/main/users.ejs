<!DOCTYPE html>
<html lang="en">
    <head><%- include('../partials/head')  %> 
        
    </head>
    <body>
        <%- include('../partials/header')  %> 

        <div>
            <% users.forEach(({name,city},idx) =>{%>
                <li><%= idx+1 %>. <%= name %>is from<%= city %>   </li>

            <%})%> 
        </div>


    </body>
</html>